# ~/.tmuxinator/work.yml
# you can make as many tabs as you wish...

project_name: dotphiles
project_root: ~/.dotphiles
socket_name: work # Not needed.  Remove to use default socket
tabs:
  - editor:
      layout: main-vertical
      panes:
        - vim
        - #empty, will just run plain shell
        - #empty, will just run plain shell
  - shell:
      layout: main-vertical
      panes:
        - #empty, will just run plain shell
        - #empty, will just run plain shell
        - #empty, will just run plain shell
  - testing:
      layout: main-vertical
      panes:
        - ssh dotphiles@localhost
        - #ssh dotphiles@centos5
        - #ssh dotphile@sdebian5
        - #ssh dotphiles@ubuntu12

